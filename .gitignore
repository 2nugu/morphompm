# build artifacts (regenerated by reproduce)
build/
*.exe
*.obj
*.pdb
*.ilk
.vs/

# python
__pycache__/
*.pyc

# keep: src, python/, tests/, include/, docs/, scripts/, and outputs/figures/
# (preserved reference artifacts: *.csv + *.png + *_desc.md)

outputs/scene/
# regenerable artifacts (scripts/reproduce.py rebuilds these); figures/ IS committed
outputs/morphompm_dashboard.html
outputs/parity_ref.csv
*.egg-info/
